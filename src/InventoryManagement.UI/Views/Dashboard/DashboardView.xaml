<UserControl x:Class="InventoryManagement.UI.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Dashboard Overview" FontSize="28" FontWeight="Bold"/>
            <Button Content="Refresh Data" Command="{Binding RefreshCommand}" Margin="20,0,0,0" Padding="15,5" Background="#17a2b8" Foreground="White" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- KPI Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sales Card -->
            <Border Grid.Column="0" Background="#28a745" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Monthly Sales" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding MonthlySales, StringFormat=C}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Purchases Card -->
            <Border Grid.Column="1" Background="#007bff" CornerRadius="8" Padding="20" Margin="10,0,10,0">
                <StackPanel>
                    <TextBlock Text="Monthly Purchases" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding MonthlyPurchases, StringFormat=C}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Alerts Card -->
            <Border Grid.Column="2" Background="#dc3545" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Low Stock Alerts" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding LowStockCount}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Lower Section -->
                <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Low Stock Items List -->
            <Border Grid.Column="0" Background="White" BorderBrush="#dee2e6" BorderThickness="1" CornerRadius="8" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border Background="#f8f9fa" Padding="15" CornerRadius="8,8,0,0" BorderBrush="#dee2e6" BorderThickness="0,0,0,1">
                        <TextBlock Text="Items Needing Attention (Low / Out of Stock)" FontSize="18" FontWeight="SemiBold"/>
                    </Border>
                    
                    <DataGrid Grid.Row="1" ItemsSource="{Binding LowStockItems}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="100"/>
                            <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="Current Stock" Binding="{Binding CurrentStock}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Reorder Level" Binding="{Binding ReorderLevel}" Width="120"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding StockStatus}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Red"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Category Analytics -->
            <Border Grid.Column="1" Background="White" BorderBrush="#dee2e6" BorderThickness="1" CornerRadius="8" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border Background="#f8f9fa" Padding="15" CornerRadius="8,8,0,0" BorderBrush="#dee2e6" BorderThickness="0,0,0,1">
                        <TextBlock Text="Products By Category" FontSize="18" FontWeight="SemiBold"/>
                    </Border>
                    
                    <DataGrid Grid.Row="1" ItemsSource="{Binding CategoryAnalytics}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="*"/>
                            <DataGridTextColumn Header="Count" Binding="{Binding ProductCount}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

