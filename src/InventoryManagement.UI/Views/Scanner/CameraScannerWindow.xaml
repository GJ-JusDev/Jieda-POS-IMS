<Window x:Class="InventoryManagement.UI.Views.Scanner.CameraScannerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InventoryManagement.UI.Views.Scanner"
        mc:Ignorable="d"
        Title="Camera Barcode Scanner" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Camera:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding AvailableCameras}" 
                      SelectedItem="{Binding SelectedCamera}" 
                      DisplayMemberPath="DisplayName"
                      Width="250"/>
            
            <CheckBox Content="Continuous Scan" 
                      IsChecked="{Binding IsContinuousScan}" 
                      VerticalAlignment="Center" Margin="20,0,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="Black" CornerRadius="4" ClipToBounds="True">
            <Grid>
                <Image Source="{Binding PreviewImage}" Stretch="Uniform"/>
                <!-- Reticle/Target overlay -->
                <Border BorderBrush="#8000FF00" BorderThickness="2" 
                        Width="250" Height="150" 
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        CornerRadius="4">
                    <Border.Background>
                        <SolidColorBrush Color="Transparent"/>
                    </Border.Background>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="{Binding StatusMessage}" 
                       VerticalAlignment="Center" 
                       FontWeight="Bold"
                       Foreground="#333333"
                       TextWrapping="Wrap"/>
            
            <Button Grid.Column="1" Content="Close" 
                    Command="{Binding CloseCommand}"
                    Width="80" Height="30"/>
        </Grid>
    </Grid>
</Window>
