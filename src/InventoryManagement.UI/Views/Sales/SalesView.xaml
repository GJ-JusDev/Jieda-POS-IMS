<UserControl x:Class="InventoryManagement.UI.Views.Sales.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Left Pane: Barcode & Items -->
            <Grid Grid.Column="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Input Area -->
                <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="15" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,10">
                    <StackPanel>
                        <!-- Dropdown Search -->
                        <WrapPanel Margin="0,0,0,15">
                            <TextBlock Text="Add Product:" VerticalAlignment="Center" FontSize="16" Width="160" Margin="0,0,10,0" FontWeight="SemiBold"/>
                            <ComboBox Width="250" Height="35" FontSize="16" Padding="5"
                                      IsEditable="True" IsTextSearchEnabled="True" TextSearch.TextPath="ProductName"
                                      ItemsSource="{Binding AvailableProducts}" 
                                      SelectedItem="{Binding SelectedProduct}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold"/>
                                            <TextBlock Text=" - SKU: " Foreground="{StaticResource TextMutedBrush}"/>
                                            <TextBlock Text="{Binding SKU}" Foreground="{StaticResource TextMutedBrush}"/>
                                            <TextBlock Text=" - " Foreground="{StaticResource TextMutedBrush}"/>
                                            <TextBlock Text="{Binding SellingPrice, StringFormat=C}" Foreground="{StaticResource PrimaryBrush}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <Button Content="Add" Command="{Binding AddSelectedProductCommand}" Margin="10,0,0,0" Padding="15,2" Style="{StaticResource PrimaryButton}" Height="35"/>
                        </WrapPanel>

                        <!-- Scanner Input -->
                        <WrapPanel>
                            <TextBlock Text="Scan Barcode / SKU:" VerticalAlignment="Center" FontSize="16" Width="160" Margin="0,0,10,0" FontWeight="SemiBold"/>
                            <TextBox Width="250" Height="35" FontSize="16" Padding="5" 
                                     Text="{Binding BarcodeInput, UpdateSourceTrigger=PropertyChanged}">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Return" Command="{Binding ScanBarcodeCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>
                            <Button Content="Add" Command="{Binding ScanBarcodeCommand}" Margin="10,0,0,0" Padding="15,2" Style="{StaticResource PrimaryButton}" Height="35"/>
                            <Button Content="📷 Camera" Command="{Binding OpenCameraScannerCommand}" Margin="10,0,0,0" Padding="15,2" Height="35" Background="#6c757d" Foreground="White" BorderThickness="0"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <!-- Cart DataGrid -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding CartItems}" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding UnitPrice, StringFormat=C}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice, StringFormat=C}" Width="100" IsReadOnly="True"/>
                        <!-- Template Column for Remove Button -->
                        <DataGridTemplateColumn Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remove" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#dc3545" Foreground="White"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- Right Pane: Checkout Summary -->
            <Border Grid.Column="1" Background="#e9ecef" Padding="20" Margin="0,10,10,10" BorderBrush="#dee2e6" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Summary" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Subtotal:" FontSize="16" HorizontalAlignment="Left"/>
                        <TextBlock Text="{Binding TotalAmount, StringFormat=C}" FontSize="16" HorizontalAlignment="Right"/>
                    </Grid>
                    
                    <Rectangle Height="1" Fill="#bdc3c7" Margin="0,10"/>
                    
                    <Grid Margin="0,0,0,30">
                        <TextBlock Text="Total:" FontSize="24" FontWeight="Bold" HorizontalAlignment="Left"/>
                        <TextBlock Text="{Binding TotalAmount, StringFormat=C}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Right" Foreground="#28a745"/>
                    </Grid>
                    
                    <Button Content="Checkout" Command="{Binding CheckoutCommand}" Height="60" FontSize="20" Background="#28a745" Foreground="White"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
