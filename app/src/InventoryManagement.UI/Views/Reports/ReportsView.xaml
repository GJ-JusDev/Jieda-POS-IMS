<UserControl x:Class="InventoryManagement.UI.Views.Reports.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#f8f9fa" Padding="10" BorderBrush="#dee2e6" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Start Date:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,15,0" Width="120"/>
                
                <TextBlock Text="End Date:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,15,0" Width="120"/>

                            <Button Content="Generate Report" Command="{Binding GenerateReportCommand}" Padding="15,5" Background="#007bff" Foreground="White" Width="120" />
            <Button Content="Export PDF" Command="{Binding ExportPdfCommand}" Padding="15,5" Background="#28a745" Foreground="White" Width="120" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Tab Control for different reports -->
        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="Stock Movement">
                <DataGrid ItemsSource="{Binding StockMovements}" AutoGenerateColumns="False" IsReadOnly="True" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=g}" Width="150"/>
                        <DataGridTextColumn Header="Transaction Type" Binding="{Binding TransactionType}" Width="120"/>
                        <DataGridTextColumn Header="Reference" Binding="{Binding Reference}" Width="150"/>
                        <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Sales Report">
                <DataGrid ItemsSource="{Binding SalesReports}" AutoGenerateColumns="False" IsReadOnly="True" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=g}" Width="150"/>
                        <DataGridTextColumn Header="Invoice No" Binding="{Binding InvoiceNumber}" Width="150"/>
                        <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/>
                        <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat=C}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Purchase Report">
                <DataGrid ItemsSource="{Binding PurchaseReports}" AutoGenerateColumns="False" IsReadOnly="True" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=g}" Width="150"/>
                        <DataGridTextColumn Header="Purchase No" Binding="{Binding PurchaseNumber}" Width="150"/>
                        <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="*"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                        <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat=C}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>


