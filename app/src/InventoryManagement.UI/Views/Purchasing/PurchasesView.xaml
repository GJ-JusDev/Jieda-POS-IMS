<UserControl x:Class="InventoryManagement.UI.Views.Purchasing.PurchasesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#f8f9fa" Padding="10" BorderBrush="#dee2e6" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="Refresh" Command="{Binding LoadCommand}" Padding="15,5" Margin="0,0,10,0"/>
                            <Button Content="Create Draft PO" Command="{Binding CreateDraftCommand}" Padding="15,5" Margin="0,0,10,0" Background="#007bff" Foreground="White"/>
            <Button Content="Edit PO" Command="{Binding EditDraftCommand}" Padding="15,5" Margin="0,0,10,0"/>
                                <Button Content="Complete PO (Stock In)" Command="{Binding CompletePurchaseCommand}" Padding="15,5" Margin="0,0,10,0" Background="#28a745" Foreground="White"/>
                <Button Content="Delete PO" Command="{Binding DeletePurchaseCommand}" Padding="15,5" Background="#dc3545" Foreground="White"/>
            </StackPanel>
        </Border>
        
        <!-- DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Purchases}" SelectedItem="{Binding SelectedPurchase}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Purchase No." Binding="{Binding PurchaseNumber}" Width="150"/>
                <DataGridTextColumn Header="Date" Binding="{Binding PurchaseDate, StringFormat=d}" Width="100"/>
                <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier.SupplierName}" Width="*"/>
                <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat=C}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>


